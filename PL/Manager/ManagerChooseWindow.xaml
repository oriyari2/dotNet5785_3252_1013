<!-- The main window for choosing between Manager and Volunteer views -->
<Window x:Class="PL.ManagerChooseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="ManagerChooseWindow" Height="200" Width="400">

    <!-- Main Grid layout for the window -->
    <Grid>

        <!-- Define two equal-sized columns -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- First column: Manager button -->
        <Grid Grid.Column="0">
            <Button HorizontalAlignment="Center" VerticalAlignment="Center" Height="100" Width="150" FontSize="25" Click="btnManagerWindow_Click">
                <!-- Button content -->
                <TextBlock Text="Manager" TextWrapping="Wrap" TextAlignment="Center"/>
            </Button>
        </Grid>

        <!-- Second column: Volunteer button -->
        <Grid Grid.Column="1">
            <Button HorizontalAlignment="Center" VerticalAlignment="Center" Height="100" Width="150" FontSize="25" Click="btnVolunteerWindow_Click">
                <!-- Button content -->
                <TextBlock Text="Volunteer" TextWrapping="Wrap" TextAlignment="Center"/>
            </Button>
        </Grid>
    </Grid>
</Window>
