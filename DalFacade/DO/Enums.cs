namespace DO; // Namespace for data objects
public enum RoleType { manager, volunteer }; // Defines user roles in the system

public enum DistanceType { air, walking, driving }; // Defines types of distances 
public enum CallType { Transportation, Babysitting, Shopping, food, Cleaning }; // Defines types of assistance requests

public enum EndType { treated, self, manager, expired } // Defines call resolution statuses


